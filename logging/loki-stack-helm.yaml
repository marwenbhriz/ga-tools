fluent-bit:
  enabled: false
promtail:
  enabled: true
  # Uncomment, if node has taint on it
  # tolerations:
  # - key: node-role.kubernetes.io/master
  #   operator: Exists
  #   effect: NoSchedule
  # - key: "type"
  #   operator: "Equal"
  #   value: "mongodb"
  #   effect: "NoSchedule"
loki:
  persistence:
    enabled: true
    size: 100Gi
  config:
    table_manager:
      retention_deletes_enabled: true
      retention_period: 168h
grafana:
  sidecar:
    datasources:
      enabled: false
